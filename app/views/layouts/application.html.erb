<!DOCTYPE html>
<html>
<head>
  <title>Kpt</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
<%= link_to('Sign out', destroy_user_session_path) if current_user %>
<%= link_to 'Top page', boards_path %>

<div id="contents">
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= yield %>
</div>
</body>
</html>
