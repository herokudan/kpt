<!DOCTYPE html>
<html>
<head>
  <title>Kpt</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
<%= link_to('Sign out', [ :destroy, current_user, :session ], :class => 'signOut') if current_user %>
<%= link_to 'Top page', people_path %>

<%= yield %>

</body>
</html>
